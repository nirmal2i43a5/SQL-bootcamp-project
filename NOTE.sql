1.Management studio :

two auth mode sql:


2.Configuration manager :


==>window auth mode() : 1)Default auth mode 
                        2)access to login  if already login to os.
                        3)

==>mix mode (sql server auth):
                            1 ) username = 'Sa' #Default
                            2 ) 


## Sql server make default 5 database :

when u create database


##-----------------Store procedure :

p--rocedure_name should be separate based on select create 
--@ = variable or paramaterl

--If no params i.e for select * //we can remove query

create procedure <procedure_name> 
(
    @Paramater datatype,
    @paramater datataype,
    -----
)
AS 
    BEGIN
        //Query(example : crud query)

        END


###select * from tbl_User;

#procedure for
 create procedure proc_User 
 AS BEGIN
  select * from tbl_User
   END;


purchase item user 


